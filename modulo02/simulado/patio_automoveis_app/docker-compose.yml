version: '3.8'

services:
  postgres:
    container_name: patio_automoveis_postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=patio_automoveis